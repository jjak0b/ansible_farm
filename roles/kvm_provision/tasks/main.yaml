# params:
# - vm: VM definition object 
# - force: boolean if true will force to override the vm resources, otherwise will use cached resources if available. xml is updated on both cases
---

- name: "VM Main lifecycle: {{ vm.metadata.name }}"
  debug:
    var: vm

- name: Install VM
  include_tasks: install/install_vm.yml
  